"""기본형 변환기 (시제품) — **메인 섹션만** 만든다.

    단순형(app/, slicer/)은 건드리지 않는다. 여기서 빌려 쓰는 것은 app.llm 뿐이다.

    흐름:  이미지 → bands.read (밴드로 자르기)
           → 【AI 1콜】 main — 스펙·핵심특징·대표컷·키피쳐·패키지·메인이 끝나는 자리
           → main.render_page (860px HTML)
"""
