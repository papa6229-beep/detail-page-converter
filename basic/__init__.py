"""기본형 변환기 (시제품).

    단순형(app/, slicer/)은 건드리지 않는다. 여기서 빌려 쓰는 것은 app.llm 뿐이다.

    흐름:  이미지 → bands.read (밴드로 자르기)
           → 【AI 1콜】 main — 스펙·핵심특징·대표컷·키피쳐·패키지·본문 시작 자리
           → 메인 = main.render_page  ·  본문 = 밴드를 **순서대로 그대로**
           → render (860px HTML)

**본문에는 판단이 없다.** 무엇이 제목이고 무엇이 사진인지 가르지 않고, 구간을 열지
않고, 번호를 붙이지 않고, 글자를 떼거나 덮거나 다시 쓰지 않는다. 그런 기준은 전부
지웠다 — 원본을 반쯤 뜯을 때마다 뜯긴 자국이 남았기 때문이다.
"""
